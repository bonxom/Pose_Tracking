# Trang profile

- lỗi: khi get user info fail (user không tồn tại / quá timeout), api bị gọi liên tục
  -> Nếu fail thì nghỉ
  Do response của BE
- ĐỔi avatar, khi BE response fail, cần fallback luôn về avatar cũ

# Trang conversation

- Tự gửi tin nhắn nhưng không auto read
- Khi click vào icon camera, mở camera, icon edit mở ra trang để tìm partner gửi tin nhắn (cần dùng api /search giống trăng block)
- Trang conversation blank (partner mới) - không có tin nhắn
- Trang khi click vào nút chữ i (giống trong slide)

# Trang home

- Sửa bài viết, load xong không hiện ui
- reload ở trang home không chuẩn (conflict cache & response data?)

# Trang noti

- lỗi: bấm vào noti user gửi tin nhắn / Yêu cầu thamg gia khóa navigate đến bài viết
- Click vào push noti comment không nhảy đến bài viết (hiện đang sang tab comment)
- Reload / thoát ra vào lại trang noti không update thời gian noti gửi đến

1 người làm kịch bản demo + test trên giao diện hiện tại và báo lỗi
-> Đức làm
